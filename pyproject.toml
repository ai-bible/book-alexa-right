[project]
name = "book-alexa-right"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp>=1.0.0",
    "pydantic>=2.0.0",
    "claude-agent-sdk>=0.1.6",
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
]
